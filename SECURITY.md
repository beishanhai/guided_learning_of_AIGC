# 安全与隐私政策

拆镜学处理的是用户上传的视频（可能含人像、声音、地点与未公开内容），因此安全与隐私是本项目的一等约束。

## 支持范围

| 版本 | 是否接受安全修复 |
|---|---|
| 最新 tag / main | 是 |
| 更早版本 | 视严重程度评估 |

## 如何报告漏洞

**请不要公开提交 issue**，改用以下任一方式：

1. GitHub 仓库的 **Private vulnerability reporting**（Security 标签页 → Report a vulnerability）；
2. 或邮件给维护者（请在仓库 README 的"维护者"一节中查收件地址，首次发布前请补上）。

请在报告中包含：影响版本/commit、复现步骤、影响面（能否越权读取他人素材、能否读取密钥、能否 RCE）、
以及你已知的缓解方式。我们会在收到后 7 天内确认，30 天内给出修复或处置计划。

## 本项目已知的安全设计（改代码时不要破坏）

- 原片与关键帧一律私有；对外访问只走**短期签名地址**（默认 300 秒，`CJX_SIGNED_URL_TTL_SECONDS`）。
- 无权限资源统一返回 **404**（不泄露资源是否存在）。
- 异步任务使用幂等键；相同键不同请求体返回 409。
- 项目删除立即撤销应用内访问，并把对象清理放进 `cleanup_tasks` 队列；Worker 写库前会再次检查删除标记。
- FFmpeg/ffprobe 一律以**参数数组**执行，不拼接 shell；每次调用有超时。
- 最多重试 2 次，仅限可重试错误（429/超时/5xx）；鉴权与参数错误直接失败，避免重复计费。
- 密钥只从服务端环境变量读取；日志过滤器会对 `token=` / `api_key` / `authorization` / `password` 做脱敏。
- **不启用任意 URL 抓取**，避免 SSRF 与内网探测。
- 不根据视频画面里的文字内容执行任何指令（提示注入只当作素材内容分析）。

## 部署者须知

1. `CJX_SECRET_KEY` 必须由密钥管理注入，不得使用默认值，不得进版本库；
2. 生产环境请设置 `CJX_ALLOW_SELF_REGISTRATION=false`；
3. 素材会发送给你配置的模型/ASR 服务，**上线前必须核对第三方数据保留与跨境条款**，并告知用户；
4. 反向代理开启 HTTPS，`client_max_body_size` 与上传上限保持一致；
5. 备份保留不超过 7 天，恢复后先应用删除记录，避免已删项目"复活"。

## 依赖与许可风险

FFmpeg 在本项目中以**独立进程**调用（不链接），因此不会把其 LGPL/GPL 条款传染到本仓库代码；
但若你改为静态链接或分发其二进制，请自行复核授权。详见 `THIRD_PARTY.md`。
