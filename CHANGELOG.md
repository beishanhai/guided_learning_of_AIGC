# 更新日志

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 与
[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 计划中
- AI 质量评测脚手架（镜头边界 ±0.3s 一对一匹配的 Precision/Recall/F1、未知率、时延与费用）
- 授权真实样本集与双人标注流程
- 受控视频生成扩展（P1）

## [0.1.0] - 2026-09-24

首个可运行原型：打通"上传参考视频 → 选择学习目标 → 查看可定位的镜头拆解与解释 →
完成迁移练习 → 提交作品 → 获得结构反馈"。

### 新增
- 后端（FastAPI + SQLAlchemy）：13 张业务表、35 个 API 端点、Alembic 初始迁移、OpenAPI 导出。
- 拆解管线：ffprobe 探测与解码校验、FFmpeg 逐帧内容变化量 + 自适应阈值切分、
  1 秒窗口累计变化量的渐变转场识别、首/中/尾 + 长镜头加密采样（全片上限 60 帧）。
- 学习引擎：镜头语言 / 叙事节奏两种意图映射、三级任务模板（模仿/变体/原创）、
  确定性结构指标、镜头对齐（允许未匹配）、证据化建议、文字分镜评分。
- 30 张已审校知识卡片（`backend/knowledge/cards.yaml`），模型只能给标签、不能自造卡片 ID。
- 私有存储与短期签名地址、幂等任务、阶段记录、陈旧锁恢复、删除即撤销访问与清理队列。
- 前端：Next.js App Router 六个页面；另提供零构建依赖的简版演示页 `/demo`。
- 一键启动/停止脚本（`scripts/*.ps1`、`web/*.ps1`、双击 `.cmd`），日志与 PID 记录在 `var/run/`。
- 测试：12 个用例覆盖 F01—F15（闭环、非法素材、无声不臆造台词、证据时间点、意图切换事实不变、
  三级任务、作业反馈、人工修正版本、幂等、重试、越权、删除、Worker 恢复、提示注入）。
- 文档：部署与运维、API 契约、技术选型记录、第三方许可清单、验收报告（含未测项）。

### 说明
- 默认 `CJX_MULTIMODAL_PROVIDER=offline`：不接入多模态模型时，景别与运镜保持 `unknown`，
  产物会显式标注离线来源，不冒充模型结果。
- 本版本**未测量** AI 质量、时延、费用与学习效果指标（缺授权真实样本集），详见 `docs/验收报告.md`。
